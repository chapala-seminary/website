{
  "name": "chapala-seminary",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "test/run-tests.sh",
    "deploy": "node tools/verify-worker-config.mjs --deploy && npm run build && wrangler deploy",
    "deploy:beta": "node tools/verify-worker-config.mjs --deploy --env beta && npm run build && wrangler deploy --env beta"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "node-html-parser": "^9.0.4"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "wrangler": "^4.135.0",
    "yaml": "^2.9.1"
  }
}
