# ---- macOS ----
.DS_Store
._*
.Spotlight-V100
.Trashes

# ---- Editors ----
.vscode/
.idea/
*.swp
*~

# ---- Node / tooling (Stage 2+) ----
node_modules/
dist/
.astro/
.cache/
*.tsbuildinfo

# ---- Cloudflare / Wrangler (Stage 2: Worker + D1) ----
.wrangler/
.wrangler-local/
# `wrangler dev` writes its build scratch next to the config it was given, and
# the test harness's config lives in test/.
test/.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example
wrangler.toml.local

# ---- Secrets ----
.env
.env.*
!.env.example
*.pem
*.key

# ---- Migration scratch ----
tools/_work/
tools/_reports/
*.orig
*.rej

# ---- Mutation harness (tools/mutation-suite.mjs) ----
# Backups of files it deliberately breaks, so an interrupted run can be undone.
.mutation-backup/
